import path from "path";

/**
 * Local on-disk roots for scraper JSON mirrors. R2 object keys are unchanged.
 */
export const pokemonLocalDataRoot = path.join(process.cwd(), "data", "pokemon");
export const onepieceLocalDataRoot = path.join(process.cwd(), "data", "onepiece");
